#include <stdio.h>
/**
* main - Entry point
* Return: Always 0 (Success)
*/
int main(void)
{
char number;
for (number = 48; number <= 5; Number ()++)
{
/*print the value of the variable*/
putchar(number);
}
/*declare a variable*/
char hexadecimal;
 for (hexadecimal = 97; hexadecimal <= 102; hexadecimal++)
{

/*print the value of the variable*/
putchar(hexadecimal);
}
putchar('\n');

return (0);
}
