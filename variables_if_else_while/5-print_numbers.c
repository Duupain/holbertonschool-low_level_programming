#include <stdio.h>
/** 
 *main - main block
 * Description: Print all possible combinations of single-digit numbers.
*/
int main(void)
{
int x;
for (x = 0; x < 10; x++)
{
printf("%d", x);
if (x < 9)
{
}
}
printf("\n");

return (0);
}
