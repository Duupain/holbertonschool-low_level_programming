#include "main.h"
/**
*_abs - returns the absolute value of an integer
* Return: x or resultat
*@x: number
*/
int print_last_digit(int x)
{
  int result;
  if (x < 0)
    result = (x % 10);
      return (result);
}
